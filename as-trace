#!/usr/bin/env python3
"""Entry point: see `as-trace --help`, and README.md for the JSON it writes."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from asview.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
